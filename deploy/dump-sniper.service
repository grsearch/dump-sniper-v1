[Unit]
Description=Dump Sniper V1 - Solana memecoin dump-rebound bot
Documentation=file:///opt/dump-sniper-v1/README.md
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=ubuntu
Group=ubuntu

# 修改为实际部署路径
WorkingDirectory=/opt/dump-sniper-v1
EnvironmentFile=/opt/dump-sniper-v1/.env

# 用绝对路径，systemd 启动时不会有 PATH
ExecStart=/usr/bin/node /opt/dump-sniper-v1/src/index.js

# 重启策略
Restart=always
RestartSec=5
StartLimitIntervalSec=60
StartLimitBurst=10

# 优雅退出超时（给 SQLite WAL flush 留时间）
TimeoutStopSec=15
KillMode=mixed
KillSignal=SIGTERM

# 日志（journalctl -u dump-sniper -f 查看）
StandardOutput=append:/opt/dump-sniper-v1/logs/out.log
StandardError=append:/opt/dump-sniper-v1/logs/err.log
SyslogIdentifier=dump-sniper

# 资源限制
LimitNOFILE=65536

# 安全加固（按需开启，可能影响 better-sqlite3 等原生模块）
NoNewPrivileges=true
ProtectSystem=full
PrivateTmp=true
# ProtectHome=true   # 如果 WorkingDirectory 在 /home 下不要开
ProtectKernelTunables=true
ProtectKernelModules=true
ProtectControlGroups=true

[Install]
WantedBy=multi-user.target
